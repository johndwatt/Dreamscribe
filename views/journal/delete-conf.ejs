<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/_head') %>
</head>
<body>
        <%- include('../partials/_navbar') %>
    <main>
        <section class="section">
            <h2>Are you sure you want to delete this journal entry?</h2>
            <section>
                <div>
                    <h3><%= journal.title %></h3>
                    <p><%= journal.createdAt %></p> 
                </div>
            </section>
            <p>(This cannot be undone. Once your dream entry is deleted it is gone forever.)</p>
            <form action="/journals/<%= journal.id %>?_method=DELETE" method="POST">
                <button type="submit" class="button">Yes, delete it!</button>
                <a href="/journals/<%= journal.id %>">Cancel</a>
            </form>
        </section>
    </main>
</body>
</html>