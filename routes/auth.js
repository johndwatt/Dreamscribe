const express = require("express");
const router = express.Router();
const controllers = require("../controllers");

// Routes
/* router.get("/", controllers.auth_ctrl.login); */


module.exports = router;